import 'package:flutter/material.dart';

class AllOrdersTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Danh sách tất cả các đơn hàng"));
  }
}
